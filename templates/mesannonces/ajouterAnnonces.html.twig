{% extends 'base.html.twig' %}

{% block title %}Ajouter une annonce{% endblock %}

{% block body %}
<h2 class="mt-3">Ajouter une annonce</h2>

<div class="container">
    
    <div class="card">
        <div class="card-body">
            {{form_start(form)}}
            {{form_widget(form)}}
            <input type="submit" class="btn btn-warning mt-3" value="Enregistrer">
            {{form_end(form)}}
        </div>
    </div>
</div>
{% endblock %}